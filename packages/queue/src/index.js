const EMAIL_QUEUE = 'email-queue';
const PRIORITY_QUEUE = 'priority-queue';
const BULK_QUEUE = 'bulk-queue';
const RETRY_QUEUE = 'retry-queue';

const EMAIL_JOB = {
  SEND_CAMPAIGN_EMAIL: 'send-campaign-email',
  SEND_BATCH_EMAIL: 'send-batch-email',
};

module.exports = {
  EMAIL_QUEUE,
  PRIORITY_QUEUE,
  BULK_QUEUE,
  RETRY_QUEUE,
  EMAIL_JOB,
};
